
class format:
    class raw_email:
        def get_approve(event_dict: dict, cookie_dict: dict)->str:
            return ""
        def get_request(event_dict: dict, cookie_dict: dict)->str:
            return ""
        def get_invite(event_dict: dict, cookie_dict: dict)->str:
            return ""
    class html_email:
        def get_approve(event_dict: dict, cookie_dict: dict)->str:
            return ""
            
        def get_request(event_dict: dict, cookie_dict: dict)->str:
            return ""
            
        def get_invite(event_dict: dict, cookie_dict: dict)->str:
            return ""

        
        
